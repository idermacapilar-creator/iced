<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Iderma Capilar Educa</title>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;800&family=Roboto+Condensed:ital,wght@1,300;1,700&display=swap" rel="stylesheet">

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Swiper -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css"/>

  <style>

    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
    }

    body{
      background:#021a78;
      overflow-x:hidden;
      font-family:'Outfit', sans-serif;
      color:white;
    }

    html.lenis{
      height:auto;
    }

    .roboto-italic{
      font-family:'Roboto Condensed', sans-serif;
      font-style:italic;
    }

    .gradient-blue{
      background:
      radial-gradient(circle at center, rgba(0,212,255,.18), transparent 40%),
      linear-gradient(180deg,#03229b 0%, #02146d 100%);
    }

    .glass{
      backdrop-filter: blur(14px);
      background: rgba(255,255,255,0.05);
      border:1px solid rgba(255,255,255,.08);
    }

    .hero-overlay{
      background:
      linear-gradient(to bottom, rgba(0,0,0,.45), rgba(0,0,0,.55)),
      linear-gradient(to right, rgba(0,21,117,.8), rgba(0,21,117,.4));
    }

    .glow-btn{
      position:relative;
      overflow:hidden;
      transition:.4s;
    }

    .glow-btn:hover{
      transform:translateY(-4px);
      box-shadow:0 0 30px rgba(255,200,0,.35);
    }

    .glow-btn::before{
      content:'';
      position:absolute;
      top:0;
      left:-120%;
      width:80%;
      height:100%;
      background:linear-gradient(
        90deg,
        transparent,
        rgba(255,255,255,.45),
        transparent
      );
      transition:1s;
    }

    .glow-btn:hover::before{
      left:130%;
    }

    .menu-panel{
      transform:translateX(100%);
      transition:1s cubic-bezier(.77,0,.175,1);
    }

    .menu-panel.active{
      transform:translateX(0);
    }

    .service-card{
      transition:.5s;
    }

    .service-card:hover{
      transform:translateY(-10px) scale(1.03);
    }

    .session-card{
      transition:.5s;
    }

    .session-card:hover{
      transform:scale(1.04);
      box-shadow:0 20px 50px rgba(0,0,0,.4);
    }

    .section-fade{
      opacity:0;
      transform:translateY(80px);
    }

    .swiper{
      overflow:visible;
    }

    .light-line{
      height:2px;
      width:100%;
      background:linear-gradient(90deg,transparent,#fff,transparent);
      opacity:.5;
    }

    .footer-bg{
      background:
      radial-gradient(circle at left, rgba(0,140,255,.25), transparent 30%),
      radial-gradient(circle at right, rgba(255,180,0,.25), transparent 30%),
      #efefef;
    }

  </style>
</head>

<body>

<!-- HEADER -->
<header class="fixed top-0 left-0 w-full z-50 px-8 py-6 flex justify-between items-center">

  <div class="text-4xl font-extrabold tracking-widest">
    EDUCA
  </div>

  <button id="menuBtn" class="space-y-2">
    <div class="w-10 h-[2px] bg-white"></div>
    <div class="w-10 h-[2px] bg-white"></div>
    <div class="w-10 h-[2px] bg-white"></div>
  </button>

</header>

<!-- MENU -->
<div id="menuPanel"
class="menu-panel fixed top-0 right-0 w-full md:w-[40%] h-screen bg-[#02146d]/95 backdrop-blur-xl z-[100] p-16">

  <div class="flex justify-end mb-20">
    <button id="closeMenu" class="text-5xl font-light">
      ×
    </button>
  </div>

  <nav class="flex flex-col gap-8 text-4xl font-light">
    <a href="#">Inicio</a>
    <a href="#">Nosotros</a>
    <a href="#">Sesiones</a>
    <a href="#">Expositores</a>
    <a href="#">Contacto</a>
  </nav>

</div>

<!-- HERO -->
<section class="relative h-screen overflow-hidden">

  <video autoplay muted loop playsinline
  class="absolute inset-0 w-full h-full object-cover">
    <source src="video.mp4" type="video/mp4">
  </video>

  <div class="hero-overlay absolute inset-0"></div>

  <!-- THREEJS -->
  <canvas id="bg"></canvas>

  <div class="relative z-10 h-full flex items-center px-10 md:px-32">

    <div class="max-w-3xl">

      <h2 class="hero-sub roboto-italic text-4xl md:text-6xl font-light mb-4">
        Compartimos
      </h2>

      <h1 class="hero-title text-6xl md:text-8xl font-extrabold leading-none">
        CONOCIMIENTO
      </h1>

      <p class="hero-text roboto-italic text-cyan-300 text-xl mt-6">
        Inspirando a nuestra comunidad a cuidarse,
        prevenir y vivir con propósito.
      </p>

      <button class="glow-btn mt-12 px-14 py-5 rounded-2xl border border-yellow-400 text-3xl roboto-italic glass">
        Ver Capítulos
      </button>

    </div>

  </div>

</section>

<!-- QUIENES SOMOS -->
<section class="section-fade py-32 px-6 md:px-20 gradient-blue">

  <h2 class="text-center text-5xl font-extrabold mb-20">
    Salud, Ciencia y Conciencia
  </h2>

  <div class="swiper whoSwiper">

    <div class="swiper-wrapper">

      <div class="swiper-slide">

        <div class="grid md:grid-cols-2 overflow-hidden rounded-[40px] glass">

          <div class="relative h-[500px]">

            <img
            src="https://images.unsplash.com/photo-1516321497487-e288fb19713f?q=80&w=1200"
            class="w-full h-full object-cover"/>

            <div class="absolute inset-0 bg-white/20 backdrop-blur-[2px]"></div>

          </div>

          <div class="p-12 flex flex-col justify-center">

            <h3 class="text-4xl font-light roboto-italic mb-8">
              Valoramos profundamente tu generosidad.
            </h3>

            <p class="text-lg text-white/80 leading-relaxed">
              Tu contribución permite que el conocimiento científico
              se integre cada vez más en la vida cotidiana.
            </p>

            <button class="glow-btn mt-10 w-fit px-10 py-4 rounded-2xl border border-yellow-400">
              Ver más
            </button>

          </div>

        </div>

      </div>

      <div class="swiper-slide">

        <div class="grid md:grid-cols-2 overflow-hidden rounded-[40px] glass">

          <div class="relative h-[500px]">
            <img
            src="https://images.unsplash.com/photo-1521737604893-d14cc237f11d?q=80&w=1200"
            class="w-full h-full object-cover"/>
          </div>

          <div class="p-12 flex flex-col justify-center">

            <h3 class="text-4xl font-light roboto-italic mb-8">
              Inspirando bienestar consciente.
            </h3>

            <p class="text-lg text-white/80 leading-relaxed">
              Especialistas nacionales e internacionales comparten
              avances médicos y prevención integral.
            </p>

          </div>

        </div>

      </div>

    </div>

  </div>

</section>

<!-- SESIONES -->
<section class="section-fade py-32 px-8 bg-[#ececec] text-black">

  <h2 class="text-center text-5xl font-extrabold max-w-5xl mx-auto mb-24">
    Especialistas comparten conocimientos, experiencias y avances médicos.
  </h2>

  <div class="grid md:grid-cols-3 gap-10 max-w-7xl mx-auto">

    <!-- CARD -->
    <div class="session-card cursor-pointer rounded-[30px] overflow-hidden bg-white shadow-2xl">

      <div class="overflow-hidden">
        <img
        src="https://images.unsplash.com/photo-1559839734-2b71ea197ec2?q=80&w=900"
        class="w-full h-[500px] object-cover"/>
      </div>

      <div class="bg-[#0024a3] text-white p-6">
        <p class="text-xl font-light">
          Glucosa, Presión y Colesterol
        </p>
      </div>

    </div>

    <div class="session-card cursor-pointer rounded-[30px] overflow-hidden bg-white shadow-2xl">

      <img
      src="https://images.unsplash.com/photo-1594824476967-48c8b964273f?q=80&w=900"
      class="w-full h-[500px] object-cover"/>

      <div class="bg-[#0024a3] text-white p-6">
        <p class="text-xl font-light">
          Nutrición y Prevención
        </p>
      </div>

    </div>

    <div class="session-card cursor-pointer rounded-[30px] overflow-hidden bg-white shadow-2xl">

      <img
      src="https://images.unsplash.com/photo-1537368910025-700350fe46c7?q=80&w=900"
      class="w-full h-[500px] object-cover"/>

      <div class="bg-[#0024a3] text-white p-6">
        <p class="text-xl font-light">
          Ciencia y Bienestar
        </p>
      </div>

    </div>

  </div>

</section>

<!-- CARRUSEL -->
<section class="section-fade py-32 gradient-blue overflow-hidden">

  <div class="max-w-7xl mx-auto px-8">

    <div class="swiper servicesSwiper">

      <div class="swiper-wrapper">

        <div class="swiper-slide">
          <div class="service-card glass rounded-[30px] p-12 h-[350px] flex items-center justify-center">
            <h3 class="text-4xl font-extrabold">
              Dermatología
            </h3>
          </div>
        </div>

        <div class="swiper-slide">
          <div class="service-card glass rounded-[30px] p-12 h-[350px] flex items-center justify-center">
            <h3 class="text-4xl font-extrabold">
              Medicina Estética
            </h3>
          </div>
        </div>

        <div class="swiper-slide">
          <div class="service-card glass rounded-[30px] p-12 h-[350px] flex items-center justify-center">
            <h3 class="text-4xl font-extrabold">
              Tricología
            </h3>
          </div>
        </div>

      </div>

    </div>

  </div>

</section>

<!-- CTA -->
<section class="relative py-32 text-center overflow-hidden">

  <div class="absolute inset-0">
    <img
    src="https://images.unsplash.com/photo-1516549655169-df83a0774514?q=80&w=1600"
    class="w-full h-full object-cover"/>
  </div>

  <div class="absolute inset-0 bg-[#d88b1f]/75 backdrop-blur-sm"></div>

  <div class="relative z-10">

    <h2 class="text-6xl font-extrabold mb-12">
      ¿Quieres formar parte de esta iniciativa?
    </h2>

    <button class="glow-btn px-16 py-5 border border-yellow-200 rounded-2xl text-3xl roboto-italic glass">
      Más información
    </button>

  </div>

</section>

<!-- FOOTER -->
<footer class="footer-bg text-[#0b227a] py-24">

  <div class="max-w-7xl mx-auto px-8 grid md:grid-cols-3 gap-20">

    <div>
      <h3 class="text-5xl font-extrabold mb-8">
        EDUCA
      </h3>

      <p class="text-xl leading-relaxed">
        Edificio Empresarial José Pardo,
        Miraflores, Lima Perú.
      </p>
    </div>

    <div>
      <h4 class="text-3xl font-extrabold mb-8">
        Navegación
      </h4>

      <ul class="space-y-4 text-2xl roboto-italic">
        <li>Capítulos</li>
        <li>Sesiones</li>
        <li>Expositores</li>
      </ul>
    </div>

    <div>
      <h4 class="text-3xl font-extrabold mb-8">
        Redes Sociales
      </h4>

      <div class="flex gap-6 text-5xl">
        <span>f</span>
        <span>♪</span>
        <span>◎</span>
      </div>
    </div>

  </div>

</footer>

<!-- LIBS -->
<script src="https://cdn.jsdelivr.net/npm/gsap@3/dist/gsap.min.js"></script>

<script src="https://cdn.jsdelivr.net/npm/gsap@3/dist/ScrollTrigger.min.js"></script>

<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

<script src="https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.42/bundled/lenis.min.js"></script>

<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

<script>

  // LENIS
  const lenis = new Lenis({
    duration:1.2,
    smoothWheel:true
  });

  function raf(time){
    lenis.raf(time);
    requestAnimationFrame(raf);
  }

  requestAnimationFrame(raf);

  // MENU
  const menuBtn = document.getElementById('menuBtn');
  const closeMenu = document.getElementById('closeMenu');
  const menuPanel = document.getElementById('menuPanel');

  menuBtn.addEventListener('click', ()=>{
    menuPanel.classList.add('active');
  });

  closeMenu.addEventListener('click', ()=>{
    menuPanel.classList.remove('active');
  });

  // HERO ANIMATIONS
  gsap.from('.hero-sub',{
    y:60,
    opacity:0,
    duration:1.2,
    ease:'power4.out'
  });

  gsap.from('.hero-title',{
    y:100,
    opacity:0,
    duration:1.5,
    delay:.2,
    ease:'power4.out'
  });

  gsap.from('.hero-text',{
    opacity:0,
    y:40,
    duration:1.2,
    delay:.6
  });

  gsap.from('.glow-btn',{
    opacity:0,
    y:30,
    duration:1.2,
    delay:.8
  });

  // SECTION FADE
  gsap.utils.toArray('.section-fade').forEach(section=>{

    gsap.to(section,{
      opacity:1,
      y:0,
      duration:1.4,
      ease:'power3.out',
      scrollTrigger:{
        trigger:section,
        start:'top 80%'
      }
    });

  });

  // SWIPERS
  new Swiper('.whoSwiper',{
    slidesPerView:1,
    spaceBetween:40,
    mousewheel:true,
    speed:1200,
    loop:true
  });

  new Swiper('.servicesSwiper',{
    slidesPerView:1.2,
    spaceBetween:30,
    mousewheel:true,
    speed:1000,
    breakpoints:{
      768:{
        slidesPerView:2.2
      },
      1200:{
        slidesPerView:3
      }
    }
  });

  // THREE PARTICLES
  const scene = new THREE.Scene();

  const camera = new THREE.PerspectiveCamera(
    75,
    window.innerWidth/window.innerHeight,
    0.1,
    1000
  );

  const renderer = new THREE.WebGLRenderer({
    canvas:document.querySelector('#bg'),
    alpha:true
  });

  renderer.setSize(window.innerWidth, window.innerHeight);

  const geometry = new THREE.BufferGeometry();
  const vertices = [];

  for(let i=0;i<2000;i++){

    vertices.push(
      (Math.random()-0.5)*2000,
      (Math.random()-0.5)*2000,
      (Math.random()-0.5)*2000
    );

  }

  geometry.setAttribute(
    'position',
    new THREE.Float32BufferAttribute(vertices,3)
  );

  const material = new THREE.PointsMaterial({
    color:0xffffff,
    size:2
  });

  const particles = new THREE.Points(geometry,material);

  scene.add(particles);

  camera.position.z = 500;

  function animate(){

    requestAnimationFrame(animate);

    particles.rotation.y += 0.0004;

    renderer.render(scene,camera);

  }

  animate();

  window.addEventListener('resize',()=>{

    camera.aspect = window.innerWidth/window.innerHeight;
    camera.updateProjectionMatrix();

    renderer.setSize(window.innerWidth, window.innerHeight);

  });

</script>

</body>
</html>